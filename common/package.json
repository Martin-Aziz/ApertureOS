{
  "name": "@pixelforge/common",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "lint": "pnpm run typecheck",
    "build": "tsc --project tsconfig.json",
    "test": "echo 'No common package tests yet'"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
